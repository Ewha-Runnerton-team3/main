{
  "name": "main",
  "version": "1.0.0",
  "description": "Main",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "start:client": "cd fe && npm start",
    "start:server": "cd server && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
